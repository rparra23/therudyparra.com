{
  "_comment": "Reviews Worker is on hold. While this points at YOUR-WORKER (or anything containing that string) the homepage falls back to mailto + manual approve via the Claude routine. To re-enable live auto-publish, change workerUrl to the deployed Worker URL (https://reviews.rparra.workers.dev) and restore the live-submit JS in index.html.",
  "workerUrl": "https://reviews.YOUR-WORKER.workers.dev"
}
