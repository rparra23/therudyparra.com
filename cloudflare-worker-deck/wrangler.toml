name = "deck"
main = "worker.js"
compatibility_date = "2025-09-01"
workers_dev = true

# KV namespace holding the slide JPEGs + unlock rate counters.
# setup.sh creates this and fills in the id automatically.
[[kv_namespaces]]
binding = "DECK"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
